interface:
  display_name: "Effect v4"
  short_description: "Build and migrate TypeScript with modular Effect guidance"
  default_prompt: "Use $effect-ts to build or migrate this TypeScript codebase with Effect v4, loading relevant internal modules and verifying behavior."
